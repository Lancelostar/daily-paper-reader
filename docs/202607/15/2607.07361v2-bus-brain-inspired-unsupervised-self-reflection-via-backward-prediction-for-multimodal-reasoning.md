---
title: "BUS: Brain-Inspired Unsupervised Self-Reflection via Backward Prediction for Multimodal Reasoning"
title_zh: BUS：基于脑启发的无监督自我反思通过反向预测实现多模态推理
authors: "Jiacheng Yang, Tongying Xiao, Yunkai Dang, Cong Wang, Yuekun Yang, Qi Fan, Tianyu Ding, Wenbin Li, Feng Miao, Yang Gao"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07361v2"
tags: ["query:vr"]
score: 8.0
evidence: 无监督自反思增强多模态视觉推理
tldr: "当前视觉语言模型在复杂视觉任务中缺乏一致且细粒度的推理能力，现有反思方法依赖大量标注数据且缺乏显式反思行为。受神经科学中向后预测机制的启发，观察到主流VLM具备向后预测能力，提出无监督自反思框架BUS，利用向后预测为反思推理提供显式学习信号，无需标注数据。在Qwen3-VL-8B上初始化，BUS在HR-Bench-8K、HR-Bench-4K、V* Bench和MME-RealWorld-Lite上分别提升8.0%、7.7%、6.3%和5.8%，证明向后预测是推进反思推理的关键。该框架模型无关且兼容SFT和RL，为多模态推理提供了一种高效的无监督增强方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07361v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 814, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2607.07361v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1733, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2607.07361v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1815, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2607.07361v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 863, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2607.07361v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 864, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2607.07361v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 297, \"height\": 300}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07361v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 1072}, {\"url\": \"assets/tables/arxiv/2607.07361v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 732}, {\"url\": \"assets/tables/arxiv/2607.07361v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 357}]"
motivation: 现有反思方法依赖大量标注数据且缺乏显式反思行为，而人类通过向后预测实现高效反思，亟需无监督的显式反思框架。
method: 提出BUS框架，利用VLM固有的向后预测能力生成无监督学习信号，通过自验证实现反思推理，兼容SFT和RL。
result: "在Qwen3-VL-8B上，BUS在HR-Bench-8K、HR-Bench-4K、V* Bench和MME-RealWorld-Lite上分别提升8.0%、7.7%、6.3%和5.8%。"
conclusion: 向后预测是推进多模态反思推理的关键机制，BUS以无监督方式有效提升VLM的复杂视觉推理性能。
---

## 摘要
当前的视觉-语言模型（VLM）在处理需要一致且细粒度推理的复杂视觉任务时常常遇到困难。最近的方法旨在训练模型以促进自我反思推理，即回顾和改进生成的推理。然而，这些方法需要大量标注数据，并且在测试时缺乏明确的反思行为。相比之下，人类通过反向预测（即预测哪些当前状态可能先于给定的未来状态）等机制进行明确且高效的自我反思。受神经科学启发，本文提出了一种新颖的解决方案来应对这些挑战。我们首先观察并研究了主流VLM能够像人脑一样进行反向预测的现象。提出了一种名为脑启发无监督自我反思（BUS）的无标签训练框架，以利用和挖掘反向预测能力来增强复杂视觉任务中的反思推理。BUS基于反向预测实现了反思推理的自我验证，在无监督条件下提供明确的学习信号。通过这种方式，BUS消除了对标注数据的依赖，同时提高了推理性能。我们的框架设计为模型无关的插件，与流行的微调方法（如监督微调（SFT）和强化学习（RL））兼容。从Qwen3-VL-8B初始化，它在HR-Bench-8K（+8.0%）、HR-Bench-4K（+7.7%）、V* Bench（+6.3%）和MME-RealWorld-Lite（+5.8%）上取得了提升，证明了反向预测是推进反思推理的关键。

## Abstract
Current Vision-Language Models (VLMs) often struggle to handle complex visual tasks that require consistent and fine-grained reasoning. Recent methods aim to train models to facilitate self-reflective reasoning, i.e., reviewing and improving the generated reasoning. However, they require large volumes of annotated data and lack explicit reflective behavior during test time. By contrast, humans perform explicit and efficient self-reflection through mechanisms such as backward prediction, i.e., predicting which current states are likely to precede a given future state. Inspired by neuroscience, this work proposes a novel solution to address these challenges. We first observe and investigate the phenomenon that mainstream VLMs can perform backward prediction, similar to the human brain. A label-free training framework named Brain-inspired Unsupervised Self-reflection (BUS) is proposed to leverage and exploit backward prediction capability to enhance reflective reasoning in complex visual tasks. BUS enables self-verification of reflective reasoning based on backward prediction, providing explicit learning signals under unsupervised conditions. In this way, BUS eliminates reliance on annotated data while improving reasoning performance. Designed as a model-agnostic plug-in, our framework is compatible with popular fine-tuning methods, such as Supervised Fine-Tuning (SFT) and Reinforcement Learning (RL). Initialized from Qwen3-VL-8B, it improves HR-Bench-8K (+8.0%), HR-Bench-4K (+7.7%), V* Bench (+6.3%), and MME-RealWorld-Lite (+5.8%), proving backward prediction is key to advancing reflective reasoning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：当前的视觉‑语言模型（VLM）在处理需要一致、细粒度推理的复杂视觉任务时表现不佳。已有的自我反思方法（如通过监督微调或提示）要么依赖大规模人工标注的反思数据，要么在测试时缺乏**显式的反思行为**（即模型仅做前向预测，并不主动回顾推理过程）。
- **背景**：人类大脑通过**反向预测（backward prediction）**（预测哪些当前状态可能先于某个未来状态）实现高效、显式的自我反思。这种机制由眶额皮层（OFC）、海马（HPC）、背外侧纹状体（DLS）和背内侧纹状体（DMS）协同完成。
- **核心研究问题**：当前 VLM 是否具备反向预测能力？如何利用该能力增强自我反思推理，从而摆脱对标注数据的依赖并实现**测试时显式反思**？

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：受脑科学启发，利用 VLM 自身已具备的反向预测能力，通过无监督方式构建自我验证信号，驱动模型在训练和推理中执行显式的反思。
- **方法流程**：
  1. **验证反向预测能力**：设计离散状态空间任务（图2），通过区分前向预测（基于后继表示 SR）和反向预测（基于前驱表示 PR）的期望值差，发现主流 VLM（Qwen3‑VL‑8B 等）的决策中有 ≥65% 符合反向预测。
  2. **BUS 框架**（图3）：
     - **Stage I：重复采样**。对输入 $x_{I\&T}$（图像+问题），用当前策略 $\pi_\theta$ 采样 $n$ 个推理‑答案对 $\{(y_i, a_i)\}_{i=1}^n$。按相同答案分组得类别 $\{c_j\}$。
     - **Stage II：无监督训练**。针对每个答案类别 $c_j$，构造新问题 $x'_{I\&T}$：“原始问题：...，模型的答案是 $c_j$，下列哪些推理可能导出该答案？选项：$y_1,\dots,y_n$”。模型进行反向预测输出 $a'$。真实标签 $a'_g = \{y_i \mid a_i = c_j\}$。通过比较 $a'$ 与 $a'_g$ 得到显式学习信号，无需外部标注。
  3. **兼容的优化算法**：
     - **BUS‑SFT**：直接最小化交叉熵损失 $\mathcal{L}_{\text{BUS-SFT}}(\theta) = -\sum \log \pi_\theta(a'_g \mid x'_{I\&T})$。
     - **BUS‑GRPO**：采用 GRPO 算法，对每个 $x'_{I\&T}$ 生成 $G$ 个候选答案，使用部分奖励函数 $R(a', a'_g)$（根据正确推理子集所占比例奖励），然后按 GRPO 更新策略。
     - **理论支撑**：通过贝叶斯定理推导，优化目标等价于最小化模型预测偏差 $\mathbb{E}_{y,c}[\log p_\theta(c \mid y, x_{I\&T})]$，促使模型学习推理与答案的一致性关系。

### 3. 实验设计：数据集、基准测试、对比方法

- **数据集/基准**：
  - **细粒度视觉推理**：
    - MME‑RealWorld‑Lite（ID）：~2K 分辨率，作为无监督训练集（无标签）和评测集。
    - HR‑Bench‑4K / 8K（OOD）：4K 和 8K 高分辨率图像。
    - V* Bench（OOD）：平均 2,246×1,583 分辨率。
  - **通用多模态推理**：MathVerse, MathVista, WeMath, MMStar。
- **对比方法**：
  - **开源通用模型**：InternVL3‑8B, Qwen2.5‑VL‑7B, Qwen3‑VL‑4B/8B。
  - **已有自我反思方法**：MIRROR, V‑Reflection (均需标注)。
  - **有监督微调方法**：PixelReasoner, DeepEyes, Thyme, TreeVGR, TikArt, SIEVE。
  - **私有模型**：GPT‑4o, GPT‑5, Gemini‑2.5‑pro/flash。
- **训练设置**：
  - 基模型：Qwen3‑VL‑8B（也扩展至 32B）。
  - 框架：基于 Transformer Reinforcement Learning (TRL) 库进行分布式训练。
  - 采样数 $n=8$；进一步分析了 $n=2,4$ 的敏感性。

### 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或训练时长，仅提到采用 TRL 框架进行分布式训练。考虑到模型为 8B 参数，通常数小时至一天即可完成单轮无监督训练，但具体细节缺失。

### 5. 实验数量与充分性

- **主要实验**（表1、表2）：在 5 个细粒度基准和 4 个通用基准上评估，对比了 10+ 种方法（含有监督和无监督）。
- **消融与分析**：
  - 不同训练数据策略：仅使用错误答案 vs. 混合正确/错误答案（表3）。
  - 采样数量 $n$ 的敏感性（表3）。
  - 预测一致性量化：图6 显示 BUS‑SFT/GRPO 显著提升反向预测准确率（+38.8%/+48.6%）。
  - 可视化案例（图5）：展示模型在选择正确前提推理上的改进。
  - 扩展性实验：在 Qwen3‑VL‑32B 上应用 BUS‑SFT，在 MME‑RealWorld‑Lite 上取得 58.8%（提升 6.8%）。
- **充分性评价**：实验覆盖了**ID 和 OOD** 场景、不同分辨率、不同基模型（7B/8B/32B），消融覆盖关键设计选择（训练信号质量、采样数），对比基线包括近期最强有监督和无监督方法。整体实验系统、客观，足以支撑结论。

### 6. 论文的主要结论与发现

1. 主流 VLM 具备与人类类似的**反向预测能力**，可通过前驱表示（PR）进行决策。
2. 利用反向预测可实现**无监督的自我反思训练**：BUS 框架通过自验证提供显式学习信号，无需任何人工标注。
3. BUS 作为模型无关插件，兼容 SFT 和 RL，在多个高阶视觉推理基准上显著提升性能：
   - HR‑Bench‑8K +8.0%，HR‑Bench‑4K +7.7%，V* Bench +6.3%，MME‑RealWorld‑Lite +5.8%。
4. 训练数据即使仅包含错误答案也能带来提升，但混合正确答案效果更优；采样数 $n$ 在 2~8 之间鲁棒。
5. 反向预测是推进多模态自我反思的关键机制，为无监督推理增强提供了有效路径。

### 7. 优点

- **无标注依赖**：真正实现了零标注训练，大幅降低数据成本。
- **显式反思行为**：通过反向预测构造新问题，强制模型在训练和推理阶段执行显式的推理回顾，而非隐式改善。
- **模型无关且兼容主流微调**：可作为即插即用模块与 SFT 或 RL（GRPO）结合，易于推广到不同 VLM。
- **理论清晰**：从贝叶斯角度给出了优化等价性解释，方法可解释性强。
- **实验充分且结果显著**：在 8 个基准上超越多种有监督/无监督方法，且能扩展到更大模型。

### 8. 不足与局限

- **算力细节缺失**：未报告训练使用的 GPU 类型、数量、时间，不利于复现和能耗评估。
- **依赖多次采样**：在 Stage I 需对每个样本采样 $n$ 次（实验中 $n=8$），推理成本增加；虽然可调节，但未讨论效率‑性能折衷。
- **训练数据选择局限**：仅用了 MME‑RealWorld‑Lite 作为无监督数据源，未验证在其他无标签数据集上的泛化效果。
- **未充分对比同类无监督方法**：目前缺少其他无监督自我反思方法的直接比较（因该领域尚少）。
- **实验覆盖领域有限**：虽然包含数学、通用推理，但未涉及医学影像、遥感等专业领域，泛化性有待验证。
- **偏差风险**：反向预测信号质量依赖于初始模型采样质量；若基模型初始偏差大，可能影响训练效果（但论文已显示即使错误答案也能有提升，一定程度上减轻此担忧）。

（完）
