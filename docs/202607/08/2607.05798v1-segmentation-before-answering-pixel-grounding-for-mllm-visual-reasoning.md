---
title: "Segmentation before Answering: Pixel Grounding for MLLM Visual Reasoning"
title_zh: 回答前分割：用于MLLM视觉推理的像素定位
authors: "Yake Wei, Yuan Wang, Fengyun Rao, Jing Lyu, Di Hu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05798v1"
tags: ["query:vr"]
score: 9.0
evidence: 基于像素的MLLM视觉推理
tldr: 多模态大语言模型（MLLM）在视觉推理中常通过边界框定位感兴趣区域，但易受背景干扰。本文提出SegAnswer，将zoom-in的单位从边界框提升为像素级分割掩码，以精细隔离目标并过滤冗余。实验表明，该方法在高分辨率感知、通用感知和幻觉测试等基准上均取得一致改进，同时在分割任务上表现良好。贡献在于实现了更精确的像素级区域定位，提升了MLLM的视觉推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05798v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 1341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1700, \"height\": 1421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1729, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05798v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 817, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1865, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1696, \"height\": 481, \"label\": \"Table\"}]"
motivation: 现有MLLM使用边界框进行区域聚焦，但边界框包含冗余背景，干扰精细推理，需要更精确的像素级定位。
method: 提出SegAnswer，用像素级分割掩码替代边界框作为zoom-in单位，隔离目标区域并直接输入MLLM，利用位置编码对齐视觉令牌。
result: 在多个基准（高分辨率感知、通用感知、幻觉测试）上取得一致改进，且分割任务性能显著，验证了像素级定位的有效性。
conclusion: SegAnswer通过像素级分割改进了MLLM的视觉推理准确性和鲁棒性，为细粒度区域理解提供了新范式。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展已从静态感知发展到交错视觉-语言推理，通常称为“用图像思考”。该推理过程中的一个基本操作是放大感兴趣区域（通常用边界框表示）以获取更精细的视觉细节。本文提出回答前分割（SegAnswer），它将放大单元从流行的边界框转移到像素级分割掩码。通过使用细粒度掩码将目标区域与杂乱环境隔离，分割后的视觉输入产生更精确的感兴趣区域，有效过滤冗余背景和干扰物体。此外，分割视觉输入的离散补丁更自然地与MLLMs通过位置嵌入组织视觉令牌的方式对齐。实验中，我们在包括高分辨率感知、通用感知和幻觉在内的多个基准上评估SegAnswer。它取得了一致的改进，并在分割任务上表现出相当的性能，验证了其可靠的像素定位能力。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have evolved from static perception to interleaved visual-language reasoning, often referred to as ``thinking with images''. A basic operation in this reasoning process is to zoom in on regions of interest (often represented with bounding boxes) to acquire finer visual details. In this paper, we propose \textbf{Seg}mentation before \textbf{Answer}ing (SegAnswer), which shifts the unit of zoom-in from the popular bounding box to pixel-level segmentation mask. By employing fine-grained masks to isolate the target area from cluttered environments, segmented visual input yields a more precise region of interest, effectively filtering out redundant background and interfering objects. Furthermore, the discrete patches of segmented visual input align more seamlessly with how MLLMs structure visual tokens via positional embeddings. In experiments, we evaluate SegAnswer across diverse benchmarks, including high-resolution perception, general perception, and hallucination. It achieves consistent improvements and also exhibits considerable performance on segmentation tasks, validating its capability for reliable pixel grounding.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：多模态大语言模型（MLLM）在“用图像思考”的视觉推理过程中，通常依赖边界框（Bounding Box）来放大感兴趣区域以获取细粒度细节。但边界框是刚性的矩形，无法适应自然物体不规则的形状，会不可避免地包含冗余背景；在物体重叠的场景下，边界框也难以分离目标与相邻实体，导致语义干扰。
- **动机**：需要一种更精确的区域定位方式，以消除背景噪声和语义混淆，从而提升MLLM在高分辨率、细粒度视觉推理任务中的表现。
- **整体含义**：本文提出将放大单位从粗粒度的边界框提升为**像素级分割掩码**，实现更精准的“像素定位”，作为视觉推理的前置步骤，从而改善MLLM的视觉感知与推理质量。

## 2. 方法论：核心思想、关键技术细节与流程
- **核心思想**：用像素级分割掩码代替边界框来定义感兴趣区域，在放大时只保留目标像素，过滤背景和干扰物体。同时，将分割后的图像补丁在原图中的位置索引保持不变，使稀疏的视觉令牌仍能有效反映空间关系，且更自然地与MLLM通过位置嵌入组织视觉令牌的方式对齐。
- **关键技术细节**：
  - **基座模型**：Qwen2.5-VL-7B。
  - **分割模块**：集成一个简单MLP作为投影器，以及SAM 2.1作为掩码解码器。使用特殊令牌 `<|seg|>` 作为接口，提取该令牌对应的隐藏状态，经投影器映射后输入掩码解码器生成分割掩码。
  - **训练三阶段**：
    1. **Pixel Grounding（像素定位阶段）**：通过多个分割数据集训练模型具备语义分割能力。优化目标为下一词预测损失 + SAM 2.1的分割损失（focal loss、dice loss、IoU MAE损失、交叉熵损失）。先冻结所有参数仅训练投影器，再使用LoRA微调MLLM并联合训练投影器和掩码解码器。训练后合并LoRA权重。
    2. **Multimodal Interleaved SFT（多模态交错监督微调阶段）**：使用VisualCOT数据集，训练模型在多轮对话中将分割作为一个中间步骤：先判断是否需要分割，生成 `<|seg|>` 令牌，然后使用分割后的图像作为新的视觉输入，结合原图回答最终答案。保持分割图像补丁的原图位置索引。仅优化MLLM骨干（冻结投影器和掩码解码器），只使用下一词预测损失。
    3. **Reasoning with Pixel Grounding（基于像素定位的推理阶段）**：通过强化学习（使用DAPO算法）优化推理策略。奖励函数包括准确率奖励 `R_acc` 和格式奖励 `R_format`。状态空间包括文本令牌和原始/分割图像视觉令牌。损失计算仅针对模型预测的令牌（屏蔽观察输入）。
- **公式/算法流程（文字描述）**：
  - 每个推理轨迹定义为文本和视觉历史的交错序列。
  - 总奖励：\( R(\tau) = w_a R_{acc}(\tau) + w_f R_{format}(\tau) \)。
  - 使用DAPO算法进行策略优化，通过token-wise masking确保观测输入不参与梯度计算。

## 3. 实验设计
- **使用的数据集/场景**：
  - **Stage 1（分割训练）**：RefCOCO、RefCOCO+、RefCOCOg、RefClef、ReasonSeg、ADE20K、COCOStuff、Mapillary Vistas、PACO-LVIS、PASCAL-Part。
  - **Stage 2（交错微调）**：VisualCOT（438k VQA对，仅使用最终答案监督，不使用边界框标注）。
  - **Stage 3（强化学习）**：ViRL39K（39k可验证VQA对）。
- **评估基准**：
  - **高分辨率感知**：V*、HR-Bench 4K、HR-Bench 8K。
  - **通用感知**：MMBench、VisuLogic、MMVP。
  - **幻觉评估**：POPE、HallusionBench。
  - **分割能力评估**：RefCOCO、RefCOCO+、RefCOCOg。
- **对比方法**：
  - **基线MLLM**：LLaVA-OneVision-9B、Qwen2.5-VL-7B。
  - **视觉推理方法**：Pixel Reasoner、DeepEyes（均使用BBox-based zoom-in）。
  - **V*专门方法**：SEAL、DyFo、Chain-of-Focus。
  - **分割专用方法**：LISA、Groundhog、LaSagnA、VideoLISA、VISA、Vitron。
- **评估方式**：重新评估了对比方法（标注†），确保公平性。

## 4. 资源与算力
- **文中说明**：论文未明确提及使用的GPU型号、数量、总训练时长等具体算力信息，仅提供了各阶段的超参数（学习率、epoch数、rollout数等）。因此无法量化算力投入。

## 5. 实验数量与充分性
- **实验组数**：共在**11个基准**上进行了定量评估（3个高分辨率、3个通用感知、2个幻觉、3个分割），此外还有定性分析（4个可视化示例）和训练奖励曲线分析。
- **充分性分析**：
  - **覆盖广泛**：涵盖了高分辨率细粒度感知、通用视觉感知、幻觉检测和分割能力四个维度，实验设计全面。
  - **对比公平**：所有对比方法均使用官方模型和代码重新评估，且在同一基座上与相似方法（Pixel Reasoner、DeepEyes）直接比较。
  - **消融实验缺失**：论文没有设置消融实验（例如，是否使用RL的对比、不同训练阶段的贡献、不同分割模型的效果等），这使得方法中各模块的具体贡献不够清晰。不过，通过对比基座Qwen2.5-VL-7B（未使用像素定位）可以部分体现整体增益。
  - **客观性**：主要结果表格和定性分析支持了结论，学习曲线展示了训练稳定性。总体而言，实验较为充分，但缺少消融研究是一个可改进之处。

## 6. 主要结论与发现
- 在**高分辨率感知**基准（V*, HR-Bench 4K/8K）上，SegAnswer相比基座Qwen2.5-VL-7B分别提升+8.9、+6.6、+8.6，并超越所有BBox-based的视觉推理方法。
- 在**通用感知**和**幻觉**基准上，SegAnswer也取得一致改进（平均+2~4.5分）。
- 在**分割任务**上，SegAnswer在RefCOCO、RefCOCO+、RefCOCOg上均超过现有分割专用方法，验证了像素定位能力的可靠性。
- 定性分析显示：模型能够自主决定何时使用分割（例如对单个或多个目标），并且分割掩码能精确隔离目标，有效消除背景干扰。
- RL训练中准确率奖励稳步提升，格式奖励始终较高，说明第二阶段的初始化有效，避免了冷启动问题。

## 7. 优点
- **创新性**：首次将视觉推理中的zoom-in单位从边界框提升到像素级分割掩码，更贴合自然物体的不规则形状和重叠场景。
- **方法设计**：三阶段训练策略逻辑清晰——先赋予分割能力，再教会模型将其作为推理中间步骤，最后用RL优化决策，结构合理。
- **空间关系保持**：通过保留分割图像补丁在原图中的位置索引，使得稀疏像素仍能反映空间关系，与MLLM的位置嵌入机制自然对齐。
- **泛化能力**：尽管主要面向高分辨率细粒度推理，但在通用感知、幻觉检测和分割任务上也表现优异，证明像素定位带来的干净视觉输入对整个视觉感知均有帮助。
- **实验公平性**：所有对比方法均使用官方模型重新评估，标注清晰，结果可信。

## 8. 不足与局限
- **计算开销**：生成分割掩码需要额外的前向推理（SAM 2.1），相比纯边界框方法可能增加推理时间和显存占用。论文未报告推理速度或效率比较。
- **依赖分割质量**：最终推理效果高度依赖于分割模型的精度。如果分割不准确或遗漏目标，可能反而引入错误信息。论文仅使用SAM 2.1，未探讨其他分割骨干或集成多个分割模型的效果。
- **消融实验缺失**：缺少对各阶段贡献（如是否必须使用RL、是否必须保持位置索引）、不同奖励权重的影响、分割生成方式的对比等消融分析，使得方法的必要性和设计选择缺乏深入验证。
- **场景覆盖有限**：实验主要集中在视觉问答（VQA）和分割基准上，未涉及需要连续多步推理、实时交互或更广泛的多模态任务（如视频、3D等）。
- **模型规模**：仅基于7B模型实验，未探索更大规模模型的表现，结论的泛化性可能受限。
- **训练数据偏差**：训练数据（VisualCOT、ViRL39K）主要来源于特定VQA数据集，可能存在与评估基准重合的风险（尤其V*等），需注意数据泄露的可能性（论文未讨论）。

（完）
