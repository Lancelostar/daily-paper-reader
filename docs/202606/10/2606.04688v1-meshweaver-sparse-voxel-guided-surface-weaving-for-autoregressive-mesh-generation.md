---
title: "MeshWeaver: Sparse-Voxel-Guided Surface Weaving for Autoregressive Mesh Generation"
title_zh: "MeshWeaver: 稀疏体素引导的表面编织用于自回归网格生成"
authors: "Jiale Xu, Wang Zhao, Ying Shan"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04688v1"
tags: ["query:d-gen"]
score: 9.0
evidence: 自回归网格生成方法
tldr: "现有自回归网格生成方法标记化效率低，且缺乏局部几何感知指导。MeshWeaver将网格生成视为表面编织过程，直接预测下一个顶点，并引入多级稀疏体素编码器提供顶点表示、引导标记预测、约束生成。该方法实现18%的压缩比，可生成最多16K面网格，几何保真度显著优于先前工作。贡献在于实现了高效的粗到细顶点预测，紧密耦合生成模型与3D几何。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1805, \"height\": 1025}, {\"url\": \"assets/figures/arxiv/2606.04688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1787, \"height\": 719}, {\"url\": \"assets/figures/arxiv/2606.04688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1804, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2606.04688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 870, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2606.04688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1809, \"height\": 1623}, {\"url\": \"assets/figures/arxiv/2606.04688v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1796, \"height\": 351}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1801, \"height\": 138}, {\"url\": \"assets/tables/arxiv/2606.04688v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 366}, {\"url\": \"assets/tables/arxiv/2606.04688v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 326}, {\"url\": \"assets/tables/arxiv/2606.04688v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 334}]"
motivation: 现有自回归网格生成方法标记化效率低，且缺乏局部几何感知指导。
method: 提出MeshWeaver框架，以直接预测顶点替代独立坐标，并利用多级稀疏体素编码器注入几何上下文。
result: "实现18%的压缩比，支持高达16K面的网格生成，几何保真度显著优于已有方法。"
conclusion: MeshWeaver通过粗到细的顶点预测和几何感知引导，提升了自回归网格生成的效率和精度。
---

## 摘要
自回归网格生成通过将网格标记化为序列并以语言建模方式训练模型而受到关注。然而，现有方法存在两个根本限制：(i) 标记化效率低，导致标记序列过长，无法扩展到高面数网格；(ii) 缺乏几何感知引导，因为生成仅以全局形状嵌入为条件，而非局部表面线索。我们提出MeshWeaver，一个自回归框架，将网格生成视为表面编织过程，直接预测下一个顶点而非独立坐标。其核心是一个多级稀疏体素编码器，通过三种互补方式将几何上下文注入生成过程：提供体素特征作为顶点表示，通过交叉注意力引导标记预测，以及作为结构支架将生成约束在输入表面附近。我们的层次化设计使得在单步解码中实现由粗到细的顶点预测，同时将生成模型与3D几何紧密耦合。大量实验表明，MeshWeaver实现了18%的最先进压缩比，能够生成多达16K面数的网格，并且相比先前方法显著提高了几何保真度。

## Abstract
Autoregressive mesh generation has gained attention by tokenizing meshes into sequences and training models in a language-modeling fashion. However, existing approaches suffer from two fundamental limitations: (i) low tokenization efficiency, which yields long token sequences and prevents scaling to high-poly meshes, and (ii) absence of geometry-aware guidance, as generation is conditioned only on global shape embeddings rather than local surface cues. We introduce MeshWeaver, an autoregressive framework that treats mesh generation as a surface weaving process by directly predicting the next vertex instead of independent coordinates. At its core is a multi-level sparse-voxel encoder that injects geometric context into the generative process in three complementary ways: providing voxel features as vertex representations, guiding token prediction via cross-attention to voxel features, and serving as a structural scaffold that constrains generation around the input surface. Our hierarchical design enables coarse-to-fine vertex prediction in a single decoding step, while tightly coupling the generative model with 3D geometry. Extensive experiments demonstrate that MeshWeaver achieves a state-of-the-art compression ratio of 18%, can generate meshes with up to 16K faces, and significantly improves geometric fidelity over prior approaches.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：多边形网格是3D几何的核心表示，但其不规则结构难以用深度生成模型建模。现有方法多依赖隐式表示（如Marching Cubes）提取网格，但会生成过密、拓扑复杂的网格，不利后续编辑。自回归网格生成（如MeshGPT、MeshXL）通过将网格标记化为序列并训练Transformer，但仍面临两大根本限制：
  - **标记化效率低**：坐标级预测导致长序列，无法扩展到高面数网格（如>4K面）。
  - **缺乏局部几何引导**：生成仅依赖全局形状嵌入（如点云编码的全局特征），缺少精细的局部表面线索，导致几何保真度差。
- **本文动机**：为了解决上述问题，提出MeshWeaver，将网格生成重新定义为**表面编织过程**，直接预测下一个顶点（而非独立坐标），并引入**多级稀疏体素编码器**注入局部几何上下文，实现紧凑序列与高保真几何。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将自回归网格生成从“下一个坐标预测”转变为“下一个顶点预测”，并在每一预测步中融入多级稀疏体素提供的几何先验。
- **关键技术细节**：
  - **顶点级网格标记化**：采用基于面片的遍历算法（类似BPT），将网格划分为局部面片，每个面片由一个中心顶点`o_i`和周围顶点`v_ij`组成。每个顶点用**多级体素索引**表示（如两级分区：16×16×16 + 8×8×8），实现粗到细的顶点预测（先预测粗体素，再预测细体素）。整个序列形如`[BOS, o_1, v_11, ..., BOS, o_2, v_21, ..., EOS]`。该方案将压缩比降至**18%**（SOTA）。
  - **稀疏体素编码器**：对输入表面进行体素化并采样点云，通过PointNet提取每体素特征，再经稀疏注意力层得到多级体素特征`{F_0, F_1, ..., F_{L-1}}`。
  - **三种几何引导方式**：
    1. **顶点表示**：每个顶点的多级体素索引对应检索体素特征，拼接形成几何感知的顶点嵌入（非静态词向量）。
    2. **交叉注意力引导**：每级预测头中，隐藏状态作为query，对应级的体素特征作为key/value，进行交叉注意力以感知局部几何。
    3. **生成支架**：预测时，将空体素的概率设为`-∞`，强制所有预测顶点落在表面附近，避免漂移。
  - **训练加速**：采用子体积剪枝，仅采样部分子体积及其关注的顶点计算损失，减少交叉注意力计算量。
  - **推理加速**：引入交叉注意力KV缓存，体素特征只需一次映射为K/V，后续按需检索，提升约14.5%的吞吐速率。
- **公式/算法流程**（文字说明）：
  - 顶点概率建模：`p(v_j) = ∏_{l=0}^{L-1} p(v_l_j | v_<l_j)`，其中`v_0_j, ..., v_{L-1}_j`为从粗到细的体素索引。
  - 整体生成序列概率：`p(M) = ∏_{t} p(vertex_t | vertex_{<t})`，每个顶点由多级索引表示。

### 3. 实验设计：数据集、基准、对比方法
- **训练数据**：混合Objaverse++、ShapeNet、3D-Future、HSSD、ABO，筛选1K-16K面网格，共约80万网格，并做随机缩放/旋转增强。
- **评估数据集**：**Toys4K**（4000个网格，105类别），用于公平比较（因为基线方法训练子集不明确）。
- **对比方法**：MeshAnythingV2、EdgeRunner、BPT、TreeMeshGPT、Mesh-Silksong（未对比Nautilus因无可用检查点）。
- **评价指标**：
  - Chamfer Distance (CD) —— 双向平均距离
  - Hausdorff Distance (HD) —— 最差偏差
  - Normal Consistency (NC) —— 法向对齐
  - 额外：压缩比（Compression Ratio）
- **实验场景**：点云条件网格生成（从输入点云重建网格）；单独对网格标记化压缩比进行基准测试。

### 4. 资源与算力
- **模型规模**：600M参数，24层LLaMA3-style Transformer（隐层1024，RoPE），另含稀疏体素编码器和点云编码器。
- **训练配置**：8 GPU（未明确具体型号，推测为NVIDIA高端GPU），batch size每GPU 4，总batch size 32，AdamW优化器，学习率1e-4→1e-5（余弦退火），训练200K步，耗时约2周。
- **推理**：在单GPU上测试吞吐量，使用KV缓存后达到30.7 tokens/s（无缓存26.8 tokens/s）。

### 5. 实验数量与充分性
- **主要实验**：点云条件网格生成定量结果（表2）和定性结果（图5），对比5种基线方法。
- **消融实验**：
  1. **稀疏体素编码器三组件**（VF、CA、GS）的独立及联合消融（表3），证明每部分均重要。
  2. **层级分区消融**（表4）：比较三种空间分区（(16,8)、(8,16)、(8,4,4)）和两种Transformer层分配（18+6、20+4、16+8），选择最优设置。
  3. **交叉注意力KV缓存效率**：200个网格的吞吐测试，提升14.5%。
- **充分性评估**：实验较全面，覆盖了核心方法组件、关键超参数和实际推理加速。但存在一定不足：
  - 训练数据与基线方法可能不同（基线方法如MeshAnythingV2等通常在Objaverse子集训练），在Toys4K上直接评估基线预训练模型，可能存在领域偏移风险，但作者声称这是为了公平（因为基线训练子集未公开）。尽管如此，结果仍能反映相对优劣。
  - 定性结果展示了3个案例，但更多案例在补充材料中，整体覆盖率尚可。

### 6. 论文的主要结论与发现
- **压缩比SOTA**：顶点级标记化实现18%压缩比，优于此前约22%的极限（表1）。
- **几何保真度显著提升**：在CD (0.116×10⁻¹)、HD (0.087)、|NC| (0.914)上均优于所有对比方法，尤其细节重建更强（如键盘、硬币图案）。
- **稀疏体素引导有效**：消融显示VF、CA、GS三者互补，移除任一组件均导致性能下降（表3）。
- **可扩展至16K面**：因压缩效率高，能处理更复杂高面网格，而部分基线受限于序列长度只能训练≤4K面。
- **推理加速**：KV缓存提升约14.5%吞吐率。

### 7. 优点
- **方法创新性**：从坐标预测转向顶点预测是本质改变；多级稀疏体素编码器以三种方式深度融合几何先验，避免全局嵌入的模糊性。
- **效率与保真兼顾**：高压缩比支持高面数生成，同时几何细节更精细。
- **实验设计合理**：消融实验覆盖三大组件+超参数，论证充分；对推理速度也做了量化分析。
- **开源/可复现潜力**：提供了训练数据构成、超参数等细节，有利于复现。

### 8. 不足与局限
- **实验公平性存疑**：基线模型未在相同训练集上重新训练，可能存在预训练数据分布不同导致的偏差。作者虽解释了原因，但严格对比应统一训练数据。
- **依赖点云条件**：生成需输入表面（点云），缺少无条件生成或文本/图像条件生成的实验。
- **拓扑质量未评估**：仅用几何距离指标，未分析生成网格的流形性、非流形结构、三角质量等，而这是后期制作的重要考量。
- **计算资源需求高**：600M参数、8 GPU训练2周，对小团队可能难以复现；推断时虽加速但仍需较大显存。
- **压缩比可进一步优化**：作者指出若省略BOS token可进一步提升压缩比，但当前未实现，说明仍有改进空间。
- **未讨论生成多样性**：仅展示单次生成结果，未提供多样性量化评估（如覆盖率、差值）。

（完）
