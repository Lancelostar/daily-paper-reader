---
title: Latent Spatial Memory for Video World Models
title_zh: 用于视频世界模型的潜在空间记忆
authors: "Weijie Wang, Haoyu Zhao, Yifan Yang, Feng Chen, Zeyu Zhang, Yefei He, Zicheng Duan, Donny Y. Chen, Yuqing Yang, Bohan Zhuang"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09828v1"
tags: ["query:world-model"]
score: 10.0
evidence: 视频世界模型的潜在空间记忆
tldr: 现有视频世界模型使用显式点云记忆，需频繁渲染和VAE编码，计算昂贵且信息损失。本文提出潜在空间记忆，直接在扩散潜在空间构建持久3D缓存，并设计Mirage框架通过深度引导实现记忆构建与查询。相比显式基线，加速10.57倍、内存减少55倍，在WorldScore上达到SOTA。该方法消除像素空间重建瓶颈，为高效视频世界模型提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09828v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1660, \"height\": 983}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1669, \"height\": 856}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1650, \"height\": 902}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1642, \"height\": 1166}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1597, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1667, \"height\": 1809}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1619, \"height\": 858}, {\"url\": \"assets/figures/arxiv/2606.09828v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1663, \"height\": 600}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09828v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1686, \"height\": 684}, {\"url\": \"assets/tables/arxiv/2606.09828v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1334, \"height\": 503}, {\"url\": \"assets/tables/arxiv/2606.09828v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1662, \"height\": 265}, {\"url\": \"assets/tables/arxiv/2606.09828v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1662, \"height\": 354}, {\"url\": \"assets/tables/arxiv/2606.09828v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 740, \"height\": 315}]"
motivation: 现有视频世界模型依赖RGB空间显式点云记忆，计算开销大且信息损失严重。
method: 提出潜在空间记忆Mirage，通过深度引导将潜在token映射到3D，并直接在潜在空间扭曲合成新视图。
result: 相比显式基线，Mirage实现10.57倍生成加速、55倍内存减少，并在WorldScore上达到SOTA。
conclusion: 潜在空间记忆消除了像素空间重建的瓶颈，为高效视频生成提供了新方向。
---

## 摘要
在生成的帧间保持3D空间一致性的视频世界模型通常依赖于在RGB空间中构建的显式点云记忆。这种设计既计算昂贵（需要重复渲染和VAE编码），又有固有的信息损失，因为往返于像素空间会丢弃学习到的潜在表示中的丰富特征。本文引入了用于视频世界模型的潜在空间记忆（latent spatial memory），这是一个持久化的3D缓存，直接将场景信息存储在扩散潜在空间中，避免了像素空间的重建。基于此，我们提出了Mirage，一个潜在空间的空间记忆框架，通过深度引导的反投影将潜在标记提升到3D来构建记忆，并通过直接潜在空间扭曲合成新视图来查询记忆。这种统一的公式消除了像素空间重建的信息损失以及重复编码和渲染的计算负担。实验表明，相对于显式3D基线，潜在空间记忆实现了高达**10.57倍**的端到端视频生成加速和**55倍**的内存占用减少。利用扩散模型的几何先验，Mirage在WorldScore上达到了最先进的性能，并在RealEstate10K上获得了强大的重建质量。

## Abstract
Video world models that maintain 3D spatial consistency across generated frames typically rely on explicit point cloud memory constructed in RGB space. This design is both computationally expensive, requiring repeated rendering and VAE encoding, and inherently lossy, as the round trip through pixel space discards rich features of the learned latent representation. In this paper, we introduce \emph{latent spatial memory} for video world models, a persistent 3D cache that stores scene information directly in the diffusion latent space, avoiding pixel-space reconstruction. Building on this, we propose Mirage, a latent-space spatial memory framework that constructs the memory by lifting latent tokens into 3D via depth-guided back-projection and queries it by synthesizing novel views through direct latent-space warping. This unified formulation eliminates both the information loss of pixel-space reconstruction and the computational burden of repeated encoding and rendering. Experiments show that latent spatial memory achieves up to \textbf{10.57}$\times$ faster end-to-end video generation and \textbf{55}$\times$ reduction in memory footprint relative to explicit 3D baselines. Leveraging the geometric prior of the diffusion model, Mirage attains state-of-the-art performance on WorldScore and strong reconstruction quality on RealEstate10K.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：视频世界模型在生成多视角一致的视频时，需要维持3D空间一致性。现有方法通常依赖**RGB空间显式点云记忆**：将每帧RGB图像通过深度反投影构建点云，在每一目标视角下重新渲染并VAE编码以获取条件信号。这种设计存在两大瓶颈：
  - **计算开销大**：每步都需要渲染数亿个彩色点并重新VAE编码，端到端生成慢。
  - **信息损失**：RGB→潜空间往返会丢弃扩散模型自身潜空间中的丰富语义特征，且引入VAE重建误差和光栅化伪影。
- **整体含义**：本文提出**潜在空间记忆（latent spatial memory）**，将3D缓存直接构建在扩散模型的潜空间中，存储潜在特征而非RGB颜色，从而完全避免像素空间往返，大幅提升效率并保持信息保真度。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：构建一个持久化的3D缓存 `M = {(p_i, f_i)}`，其中 `p_i` 是世界坐标，`f_i` 是VAE编码的潜特征向量（C=48维）。对每个新视角，通过**单次潜分辨率投影**直接读出目标视角的潜特征张量，无需渲染和重新编码。
- **关键技术细节**：
  - **记忆构建（初始化）**：给定初始帧 `I0`，VAE编码得到潜张量 `z0`，用深度估计（DepthAnything 3）得到深度图 `D0`。将深度下采样至潜网格分辨率，通过针孔反投影公式 `π^{-1}` 将每个潜单元 (u,v) 及其深度映射到世界坐标，存储该单元的潜特征向量。
  - **记忆读取（Readout）**：对于目标视角 `(Et, Kt)`，将内存中所有点投影到目标潜网格，使用Z-buffer保留最近点，得到潜特征张量 `ˆzt` 和可见性掩码 `mt`。这些张量直接通过ControlNet风格分支注入扩散骨干。
  - **自回归缓存更新**：每生成一个chunk（9潜帧≈33 RGB帧）后，对生成帧重新估计深度、VAE编码，并将静态区域（排除动态物体和天空）的潜在特征反投影追加到缓存中。动态物体用Qwen3-VL + SAM3检测并排除。
  - **高效适配预训练模型**：分两阶段训练：第一阶段冻结骨干和VAE，仅训练ControlNet分支；第二阶段解锁rank-64 LoRA适配器，联合优化。使用流匹配目标。
- **算法流程**（文字描述）：
  1. 从输入帧 `I0` 初始化缓存 `M`。
  2. 重复以下循环直到轨迹结束：
     - 对当前chunk的每帧目标视角，从 `M` 读取潜特征张量。
     - 注入ControlNet分支，扩散骨干去噪生成潜帧。
     - 解码潜帧为RGB帧，更新缓存 `M`（排除动态/天空区域）。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - **训练**：RealEstate10K 视频库，使用DepthAnything 3估计深度和相机参数，并排除动态区域。
  - **评估**：
    - **WorldScore**：包含10项指标（静态分数/动态分数/3D一致性/光度一致性/风格质量/主题质量/运动精度/运动幅度/运动平滑度等），用于衡量世界生成质量。
    - **RealEstate10K**：提供新视图合成（PSNR、SSIM、LPIPS）和闭环一致性（PSNR_C、SSIM_C、LPIPS_C）评估。
- **对比方法**：
  - **RGB点云缓存场景生成器**：WonderJourney, InvisibleStitch, WonderWorld, Voyager, FlashWorld, LucidDreamer, Spatia。
  - **通用视频生成模型**：VideoCrafter2, EasyAnimate, Allegro, CogVideoX-I2V, Vchitect-2.0, LTX-Video, Wan2.1。
  - **RealEstate10K额外对比**：SEVA, VMem, ViewCrafter, FlexWorld。
- **实现细节**：
  - 骨干：Wan2.2-TI2V-5B（VAE压缩比 4×16×16，潜通道48）。
  - 每个chunk生成9潜帧（33 RGB帧，分辨率704×1280）。
  - 推理：UniPC流调度器，40步采样，单卡H100测量效率。

### 4. 资源与算力

- **明确说明**：训练在**32块A100 GPU**上进行，全局batch size为64。两阶段训练，第一阶段冻结骨干，第二阶段解锁LoRA。训练使用AdamW优化器，bfloat16混合精度，FSDP分片，梯度检查点。文中未提供具体训练时长（如几天）。效率测量在单张NVIDIA H100上进行。

### 5. 实验数量与充分性

- **实验数量**：
  - **主要结果**：WorldScore（表格1，对比12个方法）、RealEstate10K新视图合成和闭环（表格2，对比6个方法）。
  - **效率对比**：图5展示不同chunk数量下每帧缓存读取时间和峰值显存，对比Spatia、Gen3C、VMem。
  - **消融实验**：表格3包含5组消融（潜在vs RGB、特征上采样、无动态过滤、单阶段训练）。表格4：深度源敏感性（3种深度估计器）。表格5：深度下采样方式对孔洞率的影响。
  - **定性展示**：图4、6、7、8提供多组视频对比和闭环重新访问对比。
- **充分性评估**：
  - 覆盖了主要基准和多种基线，消融全面，验证了每个设计组件的必要性。
  - 实验设计公平：对比方法均为近年顶级工作，且在同一骨干下进行了潜在 vs RGB消融以隔离变量。
  - 效率实验清晰，测量了墙钟时间和显存增长趋势。
  - **客观性**：结果数据完整，消融中性能差异明显，支持结论。

### 6. 论文的主要结论与发现

- **主要结论**：潜在空间记忆能够完全替代RGB点云记忆，并实现**同等或更优的生成质量**，同时大幅提升效率（端到端加速10.57倍，GPU内存减少55倍）。
- **关键发现**：
  - 在WorldScore上，Mirage平均分70.36，超过所有对比方法（包括RGB缓存方法Spatia的69.73和通用视频模型）。
  - 在RealEstate10K闭环测试中，Mirage的SSIM_C（0.825）远高于Spatia（0.579）和Voyager（0.540）。
  - 潜在记忆存储的语义聚簇特征比RGB点云更丰富，有利于一致性。
  - 动态物体过滤和两阶段训练对长期稳定性至关重要。
- **效率优势**：随着轨迹延长，RGB缓存的内存和时间成本急剧上升，而Mirage增长缓慢。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **创新性**：首次将3D空间记忆完全置于扩散潜空间，消除像素往返瓶颈。
  - **统一框架**：构建（深度引导反投影）、读取（潜分辨率投影）、更新（排除动态物体）均在潜空间高效进行。
  - **通用适配**：通过ControlNet分支和LoRA微调，可轻松嵌入现有扩散模型，无需修改骨干。
  - **效率最优**：潜分辨率投影比像素渲染+VAE编码快两个数量级，且缓存体积缩小16²倍（因VAE空间步长16）。
- **实验设计亮点**：
  - 在WorldScore这种综合基准上超越所有RGB缓存方法，验证了方法的有效性。
  - 闭环测试（return trajectory）最大程度暴露几何漂移，Mirage表现最好。
  - 消融实验覆盖了关键组件（特征维度、深度源、动态过滤、训练策略），分析深入。
  - 效率对比直接测量wall-clock time和显存，并展示随轨迹增长的趋势，说服力强。

### 8. 不足与局限

- **局限明确**：
  - **动态物体处理**：当前方法排除动态物体和天空，不维护移动实体的状态。对于场景以大量运动为主的视频，缓存帮助有限。
  - **深度依赖**：构建缓存需要准确的深度和相机参数，虽然实验显示对深度源鲁棒（三种深度估计器性能接近），但在极端无纹理或反光场景中仍可能不准确。
  - **训练数据域**：主要在RealEstate10K（室内/室外房产）上训练，虽然定性展示了开放域泛化，但定量迁移性能未评估。
  - **长轨迹成本**：尽管效率大幅优于RGB缓存，但缓存大小仍随轨迹增长（线性），超长轨迹可能仍会达到内存瓶颈。
- **实验可能偏差**：所有对比方法均在固定骨干Wan2.2上实现，无法排除Re-cache方法在更大骨干上可能更优。但消融实验在同一骨干上比较了潜在 vs RGB，确保了公平性。
- **应用限制**：需要可用的深度估计器和分割模型作为预处理步骤，增加了推理流水线的复杂度。实时应用可能仍受限于深度估计的延迟。

（完）
