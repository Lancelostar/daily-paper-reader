---
title: "One Lens, Many Worlds : A Capability-Typed Interface for World-Model Interpretability"
title_zh: 单一透镜，多重世界：一种面向世界模型可解释性的能力类型接口
authors: "Bhavith Chandra Challagundla, Sanskar Pandey, Param Thakkar, Rishikesh Mallagundla, Yugandhar Reddy Gogireddy, Wenhao Lu, Hindol Roy Choudhury, Shravani Challagundla, Mohamed Deraz Nasr, Spursh Deshpande"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.09936v1"
tags: ["query:world-model"]
score: 9.0
evidence: 提出一种跨架构的世界模型可解释性能力类型接口
tldr: 世界模型架构多样（循环状态空间模型、token模型、联合嵌入预测架构等），但其解释性方法共享原语，却因现有工具依赖语言模型而重复实现。本文提出WorldModelLens，通过能力类型适配器定义统一接口（编码、转移、初始状态、采样等必需方法，以及解码、奖励等可选头），使强化学习和自监督世界模型均可使用同一钩子缓存层。该方法支持时间索引激活、想象回放和干预重放，一次编写即可应用于多种架构。实验表明，该接口能显著减少跨模型可解释性分析的重实现成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09936v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 977, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1119, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09936v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 789, \"height\": 419, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09936v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09936v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 937, \"height\": 235, \"label\": \"Table\"}]"
motivation: 现有世界模型解释性工具因假设语言模型而无法通用，导致不同架构间分析代码重复实现。
method: 提出WorldModelLens，定义能力类型适配器接口，包含必需方法和可选头，并提供统一的钩子缓存层。
result: 一次编写的分析（如探针、激活修补）可直接应用于多种世界模型，无需修改。
conclusion: 能力类型接口可捕获世界模型的共享结构，降低可解释性研究的工程负担。
---

## 摘要
世界模型现在构建于截然不同的计算基质之上。诸如PlaNet和Dreamer系列的潜在循环状态空间模型将观测压缩为循环状态；基于令牌的模型如IRIS将观测量化到学习到的码本中，并通过变换器进行自回归预测；而联合嵌入预测架构如I-JEPA则在学习到的潜在空间中预测，无需像素解码器。应用于这些模型的可解释性方法，包括探测、激活修补、稀疏自编码器和惊奇分析，共享一组共同的原语，然而每种架构都需要从头重新实现，因为现有的钩子和缓存工具假定了一个变换器语言模型，没有动作、环境步骤或想象展开的概念。我们认为，这种碎片化反映了工具本身而非模型，而世界模型的共享结构可以通过一个小的类型接口来捕捉。我们提出了WorldModelLens，一个围绕能力类型适配器组织的开源可解释性基板：每个模型实现四个必需方法（编码、转移、初始状态、采样），并通过显式的能力描述符声明一组可选头（解码、奖励、继续、行动者、评论者），使得强化学习和自监督世界模型成为一等公民，而无需模仿对方。单一的钩子和缓存层在此接口上暴露了时间索引的激活、想象展开和干预重放，使得每种分析可以只编写一次。

## Abstract
World models are now built on substantially different computational substrates. Latent recurrent state-space models such as PlaNet and the Dreamer family compress observations into recurrent states; token-based models such as IRIS quantize observations into a learned codebook and predict autoregressively with a transformer; and joint-embedding predictive architectures such as I-JEPA predict in a learned latent space with no pixel decoder. The interpretability methods applied to these models, including probing, activation patching, sparse autoencoders, and surprise analysis, share a common set of primitives, yet they are re-implemented from scratch for each architecture because existing hook-and-cache tooling assumes a transformer language model with no notion of actions, environment steps, or imagined rollouts. We argue that this fragmentation reflects the tooling rather than the models, and that the shared structure of world models is captured by a small typed interface. We present WorldModelLens, an open-source interpretability substrate organized around a capability-typed adapter: every model implements four required methods (encode, transition, initial state, sample) and declares a set of optional heads (decode, reward, continue, actor, critic) through an explicit capability descriptor, so that reinforcement-learning and self-supervised world models are first-class without either imitating the other. A single hook and cache layer exposes time-indexed activations, imagination rollouts, and intervention replay over this interface, allowing each analysis to be written once.

---

## 论文详细总结（自动生成）

# 论文总结：One Lens, Many Worlds: A Capability-Typed Interface for World-Model Interpretability

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：当前世界模型架构高度分化，包括潜在循环状态空间模型（如PlaNet、Dreamer系列）、基于令牌的变换器模型（如IRIS、Decision Transformer）和联合嵌入预测架构（如I-JEPA、TD-MPC2）。虽然这些模型的可解释性方法（如探测、激活修补、稀疏自编码器、惊奇分析）共享读取、修改激活等基本原语，但现有工具（如TransformerLens）假设为变换器语言模型设计，无法处理动作、环境步、想象展开等世界模型特有的概念。这导致每种架构都需要重新实现可解释性代码，造成碎片化。
- **整体含义**：作者认为碎片化源于工具而非模型本身，世界模型共享一个小的类型化接口。论文旨在通过设计一个统一的能力类型适配器接口，使可解释性分析只需编写一次，即可应用于多种世界模型，降低重复劳动并促进跨架构比较。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出WorldModelLens，一个开源可解释性基板，围绕**能力类型适配器**组织。每个世界模型需实现四个**必需方法**（encode、transition、initial state、sample），并通过能力描述符声明一组**可选头**（decode、reward、continue、actor、critic）。通过统一的钩子和缓存层，暴露时间索引激活、想象展开和干预重放功能，使所有分析编写一次即可适配所有架构。
- **关键技术细节**：
  - **适配器层**：定义 `BaseModelAdapter` 抽象类，子类实现必需方法并覆盖可选头。能力描述符 `WorldModelCapabilities` 包含七种布尔字段（has_decoder、has_reward_head等），自动规范化隐含关系（如声明actor则隐含使用动作）。分析代码根据能力描述符分发，而不是模型具体类。
  - **钩子缓存层**：`HookedWorldModel` 包装适配器，在每一步挂载命名钩子点，通过 `HookCacheManager` 管理缓存。钩子可指定组件名、时间步和阶段（pre/post）。提供 `run_with_cache`（记录激活至时间索引缓存）和 `run_with_hooks`（临时替换激活用于干预）。
  - **轨迹与想象**：定义 `WorldState` 和 `WorldTrajectory` 等数据结构，支持真实/想象/计划状态标记。`imagine` 函数从给定状态自动滚动转移，通过策略采样动作产生反事实未来。干预重放允许重新执行记录轨迹并叠加钩子，直接测量内部编辑的因果效应。
  - **算法示例**：激活修补（Algorithm 1）包括三个步骤：运行 `run_with_cache` 记录干净激活；从缓存取出修补位置的激活；在损坏输入上通过 `run_with_hooks` 替换该激活，测量恢复效果。其他分析如探针、稀疏自编码器、集成梯度等也都基于相同的读/写原语实现一次。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集/场景**：论文未明确指定具体数据集名称。实验部分主要针对 I-JEPA（图像预测任务）进行案例研究，评估层分辨率结构。开销测量基于 I-JEPA 适配器。未使用标准强化学习基准（如Atari、DMControl）进行下游任务评估。
- **Benchmark**：没有定义统一的 benchmark 用于对比。论文侧重于证明接口的通用性和低开销，而非在特定任务上超越其他方法。
- **对比方法**：论文未与其他可解释性工具（如TransformerLens、NNsight、Captum）进行定量比较。其论证主要基于概念设计：这些工具缺乏世界模型语义（动作、轨迹、想象展开），而 WorldModelLens 通过接口填补了这一空白。

## 4. 资源与算力
- **资源信息**：文中提到“硬件细节和平均步骤数待定”（Section 5.3），未明确说明使用的 GPU 型号、数量或训练时长。开销测量（表2）给出了每步延迟，但注明“硬件和平均细节待定”。因此，论文未披露具体算力资源，这属于局限之一。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、客观、公平
- **实验数量**：
  - 覆盖验证：列出了 8 种适配器（表1），但仅描述了 I-JEPA 的深度案例研究。
  - 案例研究：I-JEPA 层分辨率结构分析（Figure 4），比较了集成梯度归因和注意力归因的 Spearman 相关性，报告了四层的结果。
  - 开销测量：表2报告了三种条件下的每步延迟（裸适配器、挂载钩子但未激活、完整缓存）。
- **充分性与客观性**：
  - **充分性不足**：深度分析仅局限在 I-JEPA 一个家族，未展示跨家族（如 Dreamer、IRIS）的统一分析结果；未进行消融实验验证接口设计的每个组件；未与现有工具对比工程效率或计算开销。
  - **客观性**：论文诚实地指出了局限性，并明确“深度演示仅限于单一家族”。案例研究设计了合理的指标（Jaccard overlap、Spearman相关系数），并报告了置信区间，具有一定客观性。但缺少随机种子、多次运行等详细信息。
  - **公平性**：没有与其他工具的公平对比，无法断言其优于现有方法。论文主要贡献是提出新接口而非性能竞争。

## 6. 论文的主要结论与发现
- 能力类型接口（必需方法+可选头）能够统一表示不同架构的世界模型，通过适配器覆盖了8个家族。
- 在该接口上实现的单一分析（如激活修补、探针、稀疏自编码器）可直接应用于所有适配器，无需修改。
- 在 I-JEPA 上的案例研究表明，注意归因与集成梯度归因在预测器层间的一致性很弱（Spearman r 多为接近零或负值），证实了“注意力不是解释”的广泛观察。
- 钩子层非活跃状态下的开销仅约 12%，适合始终在线的可解释性遥测；而完整缓存开销约 6.7 倍，适合离线分析。

## 7. 优点：方法或实验设计上有哪些亮点
- **接口设计简洁强大**：仅四个必需方法 + 五个可选头，通过能力描述符实现强化学习和自监督模型的一等公民地位，无需互相模仿。
- **模块化三层次架构**：适配器层、钩子缓存层、分析层严格分离，降低耦合，便于扩展新模型。
- **一次编写多处运行**：所有分析（探针、激活修补、稀疏自编码器等）只实现一次，自动继承所有后端，显著减少重复工作量。
- **低开销设计**：钩子层非活跃时仅增加约12%延迟，适合集成到训练和控制循环中。
- **开源实现**：代码将开源，促进社区贡献和验证。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖不足**：深度案例研究仅针对 I-JEPA 单一模型，缺乏对 Dreamer、IRIS 等其他家族的统一分析结果。跨架构的实证迁移性尚未充分验证。
- **适配器真实性**：部分适配器是“忠实重新实现”而非直接加载原始框架的检查点，可能带来与原始实现的行为差异。尽管标记了哪些适配器加载发布权重，但未详细说明。
- **开销测量未最终确定**：表2声明“硬件细节待定”，且未提供方差、均值等统计信息，影响结果可靠性。
- **缺乏与其他工具的定量对比**：未测量 WorldModelLens 与 TransformerLens、NNsight 等在具体任务上的效率或易用性差异。
- **可能的应用限制**：接口要求模型必须能够表达为编码-转移循环。一些非典型的无编码器模型（如纯仿真模型）可能难以适配。此外，当前接口不支持多模态感知或分层潜变量结构（如 Dreamer 有 deterministic + stochastic 两部分，但通过 `joint latent` 封装）。
- **偏差风险**：作者团队可能对接口设计有先入为主的偏好，实验未进行第三方复现或独立验证。

（完）
