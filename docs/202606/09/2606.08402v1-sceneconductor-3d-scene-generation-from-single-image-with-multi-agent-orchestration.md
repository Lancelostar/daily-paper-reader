---
title: "SceneConductor: 3D Scene Generation from Single Image with Multi-Agent Orchestration"
title_zh: 场景指挥家：基于多智能体编排的单图像三维场景生成
authors: "Jeonghwan Kim, Yushi Lan, Yongwei Chen, Hieu Trung Nguyen, Chuanyu Pan, Xingang Pan"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08402v1"
tags: ["query:d-gen"]
score: 9.0
evidence: 多智能体编排从单张图像生成三维场景
tldr: 从单张图像生成完整3D场景需推断全局一致的几何与物体关系，现有方法依赖整体管线且需大量场景标注，泛化性有限。本文提出多智能体编排框架SceneConductor，将任务分解为场景初始化、环境构建和多智能体细化三阶段，并引入几何感知布局预测器利用点图稀疏先验监督。在标准数据集上，该方法在几何精度、空间一致性和感知真实感上均优于先前方法，同时降低了对场景级标注的依赖，增强了在复杂真实场景中的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08402v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 898, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08402v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08402v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1448, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08402v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1455, \"height\": 1120, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08402v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1427, \"height\": 226, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08402v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1278, \"height\": 497, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08402v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1380, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08402v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1451, \"height\": 263, \"label\": \"Table\"}]"
motivation: 现有单图像3D场景生成方法依赖整体或弱分解管线，需大量场景级标注，难以泛化到复杂真实环境。
method: 提出多智能体编排框架，分三阶段（初始化、环境构建、细化）逐步生成并优化场景，引入几何感知布局预测器以点图稀疏先验监督。
result: 在基准数据集上，该方法在几何精度、空间一致性和感知真实感上均超越现有方法，泛化鲁棒。
conclusion: 多智能体协作与几何先验指导能有效提升单图像3D场景生成的泛化性与一致性。
---

## 摘要
从单张图像生成完整的三维场景需要从固有的模糊视觉证据中推断全局一致的几何、物体关系和环境上下文。尽管在联合布局和网格生成方面取得了近期进展，现有方法通常依赖于整体或弱分解的流水线，这些流水线同时纠缠多个因素，并需要大量的场景级监督，限制了它们对复杂真实世界环境的泛化能力。我们提出了一种多智能体编排框架，将单图像三维场景生成分解为三个结构化阶段：场景初始化、环境构建和多智能体细化。初始化阶段提取图像导出的物体掩码，构建物体级三维表示，并预测初始空间布局以形成粗糙的三维场景。然后，环境构建阶段利用此初始化以及点图几何来构建支撑表面、房间边界、材质和照明的环境支架。最后，在细化阶段，规划器智能体识别结构和视觉不一致性，直接应用简单修正，并分派专业智能体进行复杂的局部修订，再重新整合到全局场景中。为了在减少对场景级注释依赖的同时提供可靠的结构初始化，我们进一步引入了一种几何感知布局预测器，该预测器由从点图导出的稀疏几何先验监督。与完全监督的布局生成器不同，该预测器可以从分割级数据训练，并鲁棒地泛化到各种真实世界场景。在基准数据集上的大量实验表明，我们的方法在几何精度、空间一致性和感知真实感方面始终优于先前的方法。

## Abstract
Generating complete 3D scenes from a single image requires inferring globally consistent geometry, object relationships, and environmental context from inherently ambiguous visual evidence. Despite recent progress in joint layout-and-mesh generation, existing methods often rely on holistic or weakly decomposed pipelines that entangle many factors at once and demand extensive scene-level supervision, limiting their generalization to complex real-world environments. We propose a multi-agent orchestration framework that decomposes single-image 3D scene generation into three structured stages: scene initialization, environment construction, and multi-agent refinement. The initialization stage extracts image-derived object masks, builds object-level 3D representations, and predicts an initial spatial layout to form a coarse 3D scene. The environment-construction stage then leverages this initialization together with point-map geometry to build an environmental scaffold of supporting surfaces, room boundaries, materials, and illumination. Finally, in the refinement stage, a planner agent identifies structural and visual inconsistencies, applies simple corrections directly, and dispatches specialist agents for complex localized revisions that are reintegrated into the global scene. To provide reliable structural initialization while reducing reliance on scene-level annotations, we further introduce a geometry-aware layout predictor supervised by sparse geometric priors derived from point maps. Unlike fully supervised layout generators, the predictor can be trained from segmentation-level data and generalizes robustly to diverse real-world scenes. Extensive experiments on benchmark datasets show that our method consistently outperforms prior approaches in geometric accuracy, spatial consistency, and perceptual realism.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：从单张图像生成完整3D场景需要从模糊的视觉证据中推断全局一致的几何、物体关系和环境上下文（如房间边界、支撑面、材质、照明等）。现有方法要么依赖整体或弱分解的流水线（如扩散模型联合生成多个物体），要么采用物体中心方法依次生成再组合，但往往需要大量场景级标注，且泛化到真实复杂环境时表现差。
- **动机**：如何设计一个可扩展、可控且能泛化的框架，在减少对昂贵场景级标注依赖的同时，生成几何准确、空间一致且视觉真实的3D场景。
- **核心思想**：将单图像3D场景生成分解为三个结构化阶段（场景初始化、环境构建、多智能体细化），并引入一个无需场景级标注的几何感知布局预测器，利用点图（point map）提供的稀疏几何先验进行监督。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **多智能体编排框架（Multi-Agent Orchestration）**：每个阶段分配专门的智能体（agent）执行特定任务，任务范围限定在所需上下文中，避免全局反复推理。
- **三个阶段**：
  1. **场景初始化**：提取图像分割掩码（Grounded-SAM） → 智能体清理掩码（合并、拆分） → 用SAM3D生成物体网格 → 用几何感知布局预测器预测初始布局（旋转、平移、缩放）。
  2. **环境构建**：基于初始化场景和预测的点图，构建房间级环境骨架：通过BEV空间计算凸包获得舞台多边形（floor plan）→ 智能体推断地面、墙壁、材质和光照。
  3. **多智能体细化**：规划器智能体检查场景，直接执行简单操作（如附着地面、对齐尺度/旋转、移除物体）；复杂操作则将相关物体分组到子场景，由专业智能体迭代优化局部配置后再整合回全局场景。

### 关键技术细节
- **几何感知布局预测器**：
  - 输入：图像、掩码、点图特征 + 物体网格体素化后编码的潜在表示。
  - 输出：每个物体的旋转 \(R_i\)（仅绕Y轴偏航）、平移 \(T_i\)、缩放 \(S_i\)，以及全局地板旋转 \(F\)（共享）。
  - 公式：最终相机坐标系下顶点 \(\hat{x}_k = F(R_i(S_i \odot x_k) + T_i)\)，分解全局地板对齐与物体自身姿态。
  - 损失函数（点图监督几何损失）：
    \[
    \mathcal{L}^i_{\text{geo}} = \frac{1}{|P_i|} \sum_{p \in P_i} \min_{\hat{x} \in \hat{M}_i} \|p - \hat{x}\|_2^2 \;+\; \lambda \left( \|\hat{b}^{xy}_i - b^{xy}_i\|_1 + |\hat{b}^{z_{\min}}_i - b^{z_{\min}}_i| \right)
    \]
    其中第一项为单侧Chamfer距离（从点图点 \(P_i\) 到预测网格 \(\hat{M}_i\)），第二项为边界框对齐损失（\(\lambda=0.1\)），仅监督近深度方向（\(z_{\min}\)），因为点图只覆盖可见表面。

### 算法流程（文字描述）
1. 输入单张RGB图像。
2. **阶段1（场景初始化）**：用Grounded-SAM提取掩码，智能体清理 → 用SAM3D生成物体网格 → 用几何感知布局预测器预测物体姿态和全局地板旋转，构建初始粗糙场景。
3. **阶段2（环境构建）**：用点图提取有效3D点 → BEV投影求凸包 → 智能体添加地面、墙壁、材质、光照。
4. **阶段3（多智能体细化）**：规划器分析图像与当前场景 → 执行简单操作（附着、对齐、移除） → 对复杂交互分组，由专业智能体在隔离子场景中迭代优化，再合并回全局场景。

## 3. 实验设计

### 数据集
- **3D-FUTURE**（室内家具数据集）：用于训练布局预测器和定量评估。
- **ScanNet**（真实3D扫描室内场景）：使用伪标签（由MetaScenes生成）和Grounded-SAM改善的掩码进行评估。
- **MIT-Indoor-67**（室内场景分类数据集）：无布局标注，用于视觉质量评估，掩码由Grounded-SAM自动提取。
- **COCO**：用于训练布局预测器（仅使用几何监督）。

### Benchmark 与对比方法
- **布局预测对比**：SceneGen（扩散模型）、3D-Fixer（粗到细补全）、SAM3D（物体中心法）。
- **完整管线对比**：VIGA（图像条件双智能体框架，使用生成器-评估器对话）。
- **消融实验**：比较有无几何损失、有无地板旋转、是否使用分割数据集（COCO）训练。

### 评价指标
- **几何指标**：Chamfer Distance (CD)、F-score、边界框IoU (IoU-B)。
- **视觉指标**：VLM评分（Qwen3.5-2B评价现实性、功能性、布局一致性、图像对齐）、CLIP分数。
- 用户研究未做，全为自动指标。

## 4. 资源与算力

**未明确说明**。论文中提到使用了Qwen3.5-27B（用于图像理解）、SAM3D（物体生成）、MoGE（点图估计），但**未提及训练布局预测器所消耗的具体GPU型号、数量、训练时长**。细化阶段在Blender和Python代码中运行，但具体算力需求未报告。

## 5. 实验数量与充分性

- **定量实验**：3组主要表格（表1、表2、表3），覆盖3个数据集，对比3-4种基线。
- **消融实验**：表4（ScanNet上消融几何损失、地板旋转、分割数据集），附有定性对比（图5）。
- **定性实验**：图3（布局预测对比）、图4（完整管线各阶段 vs VIGA）。
- **充分性**：实验覆盖面较广，包括不同场景类型（室内家具、真实扫描、多种室内类别）、不同任务（布局预测、完整场景生成）、不同指标（几何+视觉）。消融实验验证了每个组件的贡献。对比方法为最新工作（截至2026年）。**相对充分且客观公平**，但缺少用户研究或更广泛的真实场景泛化测试。

## 6. 主要结论与发现

- 提出的**多智能体编排框架**在几何精度（CD、F-score、IoU-B）和视觉质量（VLM、CLIP）上**一致超越**所有对比基线。
- **几何感知布局预测器**能够从点图稀疏先验中学习，无需场景级布局标注，且泛化到未见场景，有效提升了初始布局的准确性和一致性。
- 逐步细化（环境构建 + 多智能体细化）比整体式智能体方法（VIGA）产生更真实、布局更好的场景。
- 消融实验表明：几何损失、地板旋转约束、使用大规模分割数据集训练均有助于提升性能，其中地板旋转在结合分割数据后效果最显著。

## 7. 优点

- **结构化分解**：将复杂的单图像3D场景生成分解为三个清晰阶段，每个阶段由专用智能体完成，降低每个子问题的难度。
- **减少场景级标注依赖**：布局预测器使用点图（可由RGB图像估计）的稀疏几何先验训练，训练数据来源于分割级标注（如COCO），无需完整3D场景标注。
- **局部细化高效**：规划器将简单修正直接执行，复杂修正局限在子场景中，避免整体重复推理，节省token和时间。
- **几何约束明确**：引入地板旋转 \(F\) 和仅Y轴旋转，降低自由度，保证物体共用一致地面平面，提高稳定性。
- **实验结果全面且先进**：在多个室内数据集上取得SOTA，与当前主流方法（2025-2026年）对比。

## 8. 不足与局限

- **依赖多个基础模型**：管线顺序依赖Grounded-SAM、SAM3D、MoGE、Qwen3.5等，任一环节错误可能传播至后续阶段，且推理延迟累积。
- **场景类型受限**：当前实验仅针对室内场景（家具、房间），未测试室外或无限环境，材质建模也限于纹理层面。
- **缺乏训练资源报告**：未说明布局预测器的训练算力消耗，无法评估实际部署成本。
- **无用户研究**：视觉指标虽使用VLM评分，但缺少人类主观评价，可能存在自动评分偏差。
- **泛化性验证有限**：COCO虽用于训练，但仍属于2D分割数据，真实世界复杂场景（如极端遮挡、镜面、非刚性物体）未充分测试。
- **计算开销**：多智能体对话（尤其是复杂局部细化）虽较整体方法高效，但多次迭代仍可能增加时间成本。

（完）
